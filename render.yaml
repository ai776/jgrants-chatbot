services:
  - type: web
    name: jgrants-mcp-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m jgrants_mcp_server.core --host 0.0.0.0 --port $PORT --mode http
    port: $PORT
    envVars:
      - key: PORT
        value: 8000
